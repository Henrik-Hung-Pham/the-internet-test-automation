# Production Environment Configuration
# Override BASE_URL only — every URL_* in global_variables.resource is derived
# from ${BASE_URL}, so all pages follow the selected environment.
BASE_URL: https://the-internet.herokuapp.com
USERNAME: tomsmith
PASSWORD: SuperSecretPassword!
